<h1>New Team</h1>

<%= form_for(@team) do |f| %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>
  <div class="actions">
    <%= f.submit "Create Team" %>
  </div>
<% end %>

<%= link_to 'Back', teams_path %>
