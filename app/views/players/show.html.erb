<h1><%= @player.username %></h1>

<strong>Team:</strong>
<%= @player.team ? @player.team.name : "none" %>
</p>
<% if @player.team %>
  <%= render 'layouts/team_status', team: @player.team %>
<% else %>
  <%= link_to "Create a Team!", new_team_path, class: "btn btn-default" %>
<% end %>
